#worst
#include<stdio.h>
int main()
{
int i,j,temp,f[10],fp[10];
int no,p[15],prmem[15],part[15],pr[15],pno;
printf("Enter the no. of blocks:\n");
scanf("%d",&no);
for(i=1;i<=no;i++)
{
p[i]=i;
printf("Enter the no of files %d\t",i);
scanf("%d",&part[i]);
}
for(i=1;i<=no;i++)
{
for(j=1;j<=i;j++)
{
if(part[j]<part[i])
{
temp=part[i];
part[i]=part[j];
part[j]=temp;
temp=p[i];
p[i]=p[j];
p[j]=temp;
}
}
}
printf("\n Enter the size of blocks")
for(i=1;i<=no;i++)
{
printf("\n blocks %d:\t %d",p[i],part[i]);
}
printf("\n enter the number of process:");
scanf("%d",&pno);
for(i=1;i<=pno;i++)
{
pr[i]=i;
printf("\n enter the size of files %d:\t",i);
scanf("%d",&prmem[i]);
}
printf("\n ------------\n");
printf("process| \t partition \t free memory|\n");
printf("\n --------------\n");
j=1;
for(i=1;i<=no;i++)
{
f[i]=0;
fp[j]=0;
}
while(j<=pno)
{
for(i=1;i<=no;i++)
{
if((part[i]>=prmem[j])&&(f[i]==0))
{
part[i]=part[i]-prmem[j];
fp[j]=1;
f[i]=1;
printf("%d \t %d \t %d \n",pr[j],p[j],part[i]);
goto l1;
}
}
l1:
j++;
}
for(i=1;i<=no;i++)
{
if(f[i]==0)
{
printf("\t %d \t %d \n",p[i],part[i]);
}
}
printf("\n the following process is not allocated:");
for(i=1;i<=pno;i++)
{
if(fp[i]==0)
{
printf("%d",pr[i]);
}
}


#best
#include <stdio.h>
void main()
{
int process[5], m, n, block[5], i, j, p[5], b[5], t, t1;
printf("\nenter the number of blocks:");
scanf("%d", & n);
printf("enter size for"); for (i = 1; i <= n; i++)
{
printf("\tblock %d:", i);
scanf("%d", & block[i]); b[i] = 1;
}
printf("enter number of process:");
scanf("%d", & m);
printf("\nenter the size for");
for (i = 1; i < n; i++)
{
printf("\nprocess %d:", i);
scanf("%d", & process[i]); p[i] = p;
}
for (i = 1; i < n; i++)
{
for (j = 1; j < m; j++)
{
if (process[j] > block[i])
{
t = block[i]; block[j]; block[j] = t;
t1 = b[i]; b[i] = b[j];
b[j] = t1;
}
}
for (i = 1; i <= n; i++)
{
for (j = 1; j <= m; j++)
{
if (process[j] <= block[i])
{
printf("\nprocess %d is allocated to block %d", j, i);
process[j] = 1000;
break;
}
}
}
for (j = 1; j <= n; j++)
{
if (process[j] != 1000)
{
printf("\nprocess %d is not allocated\n", j);
}
}
}
